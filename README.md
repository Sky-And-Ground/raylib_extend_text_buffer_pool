# raylib_show_utf8_characters
##### I write this to solve the utf8 characters shown problems.
##### c version just uses a customized dynamic string and a bitset to insert unique utf8 codepoint, no 3rd-parties, C++ version does the same thing. you can just copy the code or convert it into a library for integrated.
